name: ci
on:
  pull_request:
  push:
    branches:
      - "**"
jobs:
  perl:
    name: "Perl ${{matrix.perl}}, Redis ${{matrix.redis}}"
    strategy:
      matrix:
        os: ["ubuntu-latest"]
        perl: ["5.32", "5.16"]
        redis: ["5", "latest"]
    runs-on: "${{matrix.os}}"
    steps:
      - uses: actions/checkout@v3
      - name: Setup redis
        uses: shogo82148/actions-setup-redis@v1
        with:
          redis-version: ${{ matrix.redis }}
          auto-start: true
      - uses: shogo82148/actions-setup-perl@v1
        with:
          perl-version: "${{matrix.perl}}"
          install-modules-with: cpanm
          install-modules-args: --with-develop --with-configure          
      - run: redis-cli --version
      - run: perl -V
      - name: Install dependencies
        run: |
          cpanm -n Test::CPAN::Changes Test::Pod::Coverage Test::Pod Test::Spelling
          cpanm -n --installdeps .
      - name: Run tests via TCP
        run: prove -l t/*.t
        env:
          HARNESS_OPTIONS: j4
          TEST_ONLINE: "redis://127.0.0.1:${{steps.setup.outputs.redis-port}}"
      - name: Run tests via UNIX socket
        run: prove -l t/*.t
        env:
          HARNESS_OPTIONS: j4
          TEST_ONLINE: "${{ steps.setup.outputs.redis-unix-socket }}"
